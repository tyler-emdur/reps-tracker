# Copy to .env.local and fill in values from your Upstash Redis dashboard
# (Vercel Dashboard → Integrations → Upstash Redis → your store → .env.local tab)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
